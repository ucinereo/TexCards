import { MarkedRendererWrap } from './marked-renderer-wrap';

describe('MarkedRendererWrap', () => {
  it('should create an instance', () => {
    expect(new MarkedRendererWrap()).toBeTruthy();
  });
});
