import { CreateFlashcardSetRequest } from './create-flashcard-set-request';

describe('CreateFlashcardSetRequest', () => {
  it('should create an instance', () => {
    expect(new CreateFlashcardSetRequest()).toBeTruthy();
  });
});
