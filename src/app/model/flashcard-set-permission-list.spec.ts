import { FlashcardSetPermissionList } from './flashcard-set-permission-list';

describe('FlashcardSetPermissionList', () => {
  it('should create an instance', () => {
    expect(new FlashcardSetPermissionList()).toBeTruthy();
  });
});
