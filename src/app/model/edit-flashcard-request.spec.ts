import { EditFlashcardRequest } from './edit-flashcard-request';

describe('EditFlashcardRequest', () => {
  it('should create an instance', () => {
    expect(new EditFlashcardRequest()).toBeTruthy();
  });
});
