import { RemoveReadPermissionRequest } from './remove-read-permission-request';

describe('RemoveReadPermissionRequest', () => {
  it('should create an instance', () => {
    expect(new RemoveReadPermissionRequest()).toBeTruthy();
  });
});
