import { FlashcardSetImport } from './flashcard-set-import';

describe('FlashcardSetImport', () => {
  it('should create an instance', () => {
    expect(new FlashcardSetImport()).toBeTruthy();
  });
});
