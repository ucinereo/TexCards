import { RemoveStarRequest } from './remove-star-request';

describe('RemoveStarRequest', () => {
  it('should create an instance', () => {
    expect(new RemoveStarRequest()).toBeTruthy();
  });
});
