import { EditFlashcardSetRequest } from './edit-flashcard-set-request';

describe('EditFlashcardSetRequest', () => {
  it('should create an instance', () => {
    expect(new EditFlashcardSetRequest()).toBeTruthy();
  });
});
