import { DeleteFlashcardSetRequest } from './delete-flashcard-set-request';

describe('DeleteFlashcardSetRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteFlashcardSetRequest()).toBeTruthy();
  });
});
