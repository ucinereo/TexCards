import { RemoveFlashcardRequest } from './remove-flashcard-request';

describe('RemoveFlashcardRequest', () => {
  it('should create an instance', () => {
    expect(new RemoveFlashcardRequest()).toBeTruthy();
  });
});
