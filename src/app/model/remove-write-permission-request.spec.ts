import { RemoveWritePermissionRequest } from './remove-write-permission-request';

describe('RemoveWritePermissionRequest', () => {
  it('should create an instance', () => {
    expect(new RemoveWritePermissionRequest()).toBeTruthy();
  });
});
