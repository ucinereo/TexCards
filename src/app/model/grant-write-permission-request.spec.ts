import { GrantWritePermissionRequest } from './grant-write-permission-request';

describe('GrantWritePermissionRequest', () => {
  it('should create an instance', () => {
    expect(new GrantWritePermissionRequest()).toBeTruthy();
  });
});
