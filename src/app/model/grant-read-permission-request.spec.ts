import { GrantReadPermissionRequest } from './grant-read-permission-request';

describe('GrantReadPermissionRequest', () => {
  it('should create an instance', () => {
    expect(new GrantReadPermissionRequest()).toBeTruthy();
  });
});
